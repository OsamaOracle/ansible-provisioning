version: "3.7"
services:
  minio:
    image: minio/minio
    restart: always
    environment:
      - MINIO_ACCESS_KEY={{minio_access_key}}
      - MINIO_SECRET_KEY={{minio_access_secret}}
    volumes:
      - /minio/hume_cloud_data:/data
    command: "server /data"
    ports:
      - 9000:9000
